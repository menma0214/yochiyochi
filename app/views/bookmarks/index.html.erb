<% content_for(:title, t('.title')) %>
<div class="bookmarks-container ps-2 pe-2">
  <div class="bookmarks-wrapper text-center">
    <div class="row">
      <div class="col-lg-10 offset-lg-1">
        <!-- 検索フォーム -->
        <%# <%= render 'shared/search_form', q: @q, url: bookmarks_facilities_path %>
        <%# <form>
          <div class="input-group mb-3"><input class="form-control" placeholder="検索ワード" type="search"/>
            <div class="input-group-append"><input type="submit" value="検索" class="btn btn-primary"/></div>
          </div>
        </form> %>
      </div>
    </div>
    <!-- 掲示板一覧 -->
    <div class="row">
      <div class="col-12">
        <div class="row">
          <% if @bookmarks.present? %>
            <%= render @bookmarks %>
          <% else %>
            <p><%= t('.no_result') %></p>
          <% end %>

        </div>
        <%= paginate @bookmarks %>
      </div>
    </div>
  </div>
</div>