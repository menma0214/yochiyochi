<% content_for(:title, t('.title')) %>
<div class="review-form-container ps-2 pe-2">
  <div class="row">
    <div class="col-lg-8 offset-lg-2">
      <h1><%= t('.title') %></h1>
      <%= render 'form', review: @review %>
    </div>
  </div>
  <div class="text-center">
      <%= link_to "戻る", facility_path(@facility), class:"text-end" %>
  </div>
</div>